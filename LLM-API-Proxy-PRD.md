# 产品需求文档（PRD）

## 多厂商大模型 API 统一代理网关

| 项目 | 内容 |
|------|------|
| 文档版本 | v1.5 |
| 创建日期 | 2026-07-25 |
| 产品名称 | LLM-API-Proxy（暂定） |
| 产品定位 | 统一管理多厂商大模型 API 的本地代理服务 |

---

## 一、产品背景与目标

### 1.1 背景

用户订阅了多家大模型厂商的 API 服务（如 DeepSeek、OpenAI/GPT、智谱 GLM、Grok、Claude 等），各厂商虽然兼容 OpenAI 通用 API 格式，但各自拥有独立的 URL、API Key 和模型列表。日常使用中需要频繁切换不同厂商的连接信息，操作流程繁琐且容易出错。

### 1.2 目标

构建一个本地运行的 **桌面 GUI 应用 + API 代理服务**，实现以下能力：

- **统一管理**：集中管理所有上游 API 订阅信息
- **号池聚合**：将多个厂商的同名/同类模型聚合到一个自定义号池中
- **统一接入**：对外暴露一个 OpenAI 兼容的统一 URL 和 API Key
- **智能轮询**：对同一号池的请求自动轮询不同上游，实现负载均衡与故障转移
- **模型伪装**：终端工具看到的模型名是用户自定义的名字（如 `grok-4.5`），而非底层实际模型名
- **原生 GUI 管理**：所有配置和操作在桌面窗口中完成，不需要打开浏览器或 Web 页面

### 1.3 核心价值

- ✅ 一个配置，到处使用（所有支持 OpenAI 格式的客户端都能对接）
- ✅ 自动切换，无需手动维护多套密钥和地址
- ✅ 轮询保障，某家故障时不影响使用
- ✅ 思考模式开关，按需开启深度推理
- ✅ 自定义命名，隐藏底层复杂性
- ✅ 便携部署，U 盘即插即用
- ✅ 原生窗口管理，不开浏览器、不搞混

---

## 二、术语定义

| 术语 | 说明 |
|------|------|
| 上游订阅（Upstream） | 一条具体的厂商 API 连接信息，包含厂商名、URL、API Key 和选中的实际模型 |
| 号池（Pool） | 用户自定义命名的逻辑模型集合，由多条上游订阅组成。对外表现为一个"模型" |
| 统一入口（Gateway） | 代理服务对外暴露的统一 API 地址和认证信息 |
| 轮询（Round-Robin） | 对同一号池的连续请求，按顺序分配给不同的上游订阅 |
| 模型别名（Alias） | 用户自定义的显示名称，如 `grok-4.5`，替代底层实际模型名 |
| 思考模式（Thinking Mode） | 由代理层自动注入上游 API 的思考/推理参数，使模型启用深度思考 |
| 便携模式（Portable） | 程序和数据全部跟随 exe 所在目录，无需安装任何依赖 |
| 桌面客户端（Desktop App） | 程序自带的原生 GUI 窗口，所有配置和管理操作都在这个窗口中完成 |

---

## 三、用户角色

| 角色 | 描述 |
|------|------|
| 管理员 / 个人用户 | 部署代理服务，管理上游订阅和号池配置 |

---

## 四、功能需求

### 4.1 总体功能架构

```
┌──────────────────────────────────────────────┐
│              终端工具（ChatGPT/Claude 等）       │
│        URL: http://127.0.0.1:47339/v1          │
│        API Key: sk-xxx                        │
└────────────────────┬─────────────────────────┘
                     │
                     ▼
┌──────────────────────────────────────────────┐
│           LLM-API-Proxy 桌面应用               │
│                                               │
│  ┌────────────────────────────────────────┐   │
│  │                                         │   │
│  │         原生 GUI 管理窗口                │   │
│  │   （配置/监控/日志全部在此窗口完成）      │   │
│  │                                         │   │
│  │  ┌──────────┐  ┌──────────┐  ┌───────┐ │   │
│  │  │ 上游管理  │  │ 号池管理  │  │ 日志  │ │   │
│  │  │ Upstreams │  │ Pools    │  │ Logs  │ │   │
│  │  └──────────┘  └──────────┘  └───────┘ │   │
│  │  ┌──────────┐  ┌──────────┐  ┌───────┐ │   │
│  │  │ 网关设置  │  │ 健康检查  │  │ 仪表盘│ │   │
│  │  │ Settings  │  │ Health   │  │Dash   │ │   │
│  │  └──────────┘  └──────────┘  └───────┘ │   │
│  └────────────────────────────────────────┘   │
│                                               │
│  ┌──────────┐  ┌──────────┐  ┌─────────────┐ │
│  │ 路由引擎  │  │ 轮询调度器│  │ SQLite 数据库│  │
│  │ (Router) │  │(Balancer) │  │  (DB)        │ │
│  └──────────┘  └──────────┘  └─────────────┘ │
└──────────────────────────────────────────────┘
                     │
        ┌────────────┼────────────┐
        ▼            ▼            ▼
   ┌─────────┐  ┌─────────┐  ┌─────────┐
   │ 上游A    │  │ 上游B    │  │ 上游C    │
   │DeepSeek │  │ 智谱GLM  │  │ OpenAI  │
   └─────────┘  └─────────┘  └─────────┘
```

### 4.2 上游订阅管理

#### 4.2.1 功能描述

用户可以添加、编辑、删除、启用/禁用多个 API 上游订阅。

#### 4.2.2 字段要求

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| 供应商名称 | 文本 | ✅ | 如：DeepSeek、OpenAI、智谱、Grok、Anthropic 等 |
| Base URL | 文本 | ✅ | 上游 API 的地址，如 `https://api.deepseek.com/v1` |
| API Key | 密码 | ✅ | 上游 API 的密钥 |
| 实际模型名 | 下拉选择 | ✅ | 从一键获取的模型列表中选择的模型 |
| 是否启用 | 开关 | ✅ | 默认为启用，可临时禁用某个上游 |
| 备注 | 文本 | ❌ | 可选的描述信息 |

#### 4.2.3 关键交互

1. **填写基本信息**：输入供应商名称、Base URL、API Key
2. **一键获取模型列表**：点击按钮后，使用已填写的 URL 和 Key 调用 `/v1/models`，拉取该厂商可用的模型列表
3. **选择模型并保存**：从下拉列表中选择要使用的模型，保存为一条上游订阅记录
4. **支持批量导入**：未来可考虑从一个 JSON/CSV 文件中批量导入上游配置

### 4.3 号池管理

#### 4.3.1 功能描述

用户创建一个号池（自定义模型名），并将多条上游订阅添加到该号池中。

#### 4.3.2 字段要求

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| 号池名称 | 文本 | ✅ | 自定义的模型名称，如 `grok-4.5`、`gpt-5.5`、`claude-opus-4.7` |
| 关联上游订阅 | 多选 | ✅ | 从已配置的上游订阅中选择加入本号池 |
| 默认模型别名 | 文本 | ✅ | 对外展示的模型名，通常与号池名称一致 |
| 轮询策略 | 单选 | ✅ | 当前仅支持：顺序轮询（Round Robin）。可扩展：随机、加权、最少连接 |
| 故障转移 | 开关 | ✅ | 默认为开启。当某个上游请求失败时，自动尝试下一个上游 |
| 超时时间（秒） | 数字 | ✅ | 单个上游响应超时阈值，默认 30s |
| 最大并发数 | 数字 | ❌ | 该号池同时发给上游的最大并发请求数，默认 5。设为 1 时等同于串行执行，可完全避免乱序问题 |
| 思考模式 | 开关 | ❌ | 开启后该号池下所有上游请求自动附带思考模式参数 |

#### 4.3.3 号池命名规则

- 名称只能包含字母、数字、中文字符、空格、连字符（-）和下划线（_）
- 最长 64 个字符
- 同一实例下不能重名
- 示例：`grok-4.5`、`gpt-5.5`、`claude opus 4.7`、`deepseek-v4`

### 4.3.4 思考模式（Thinking Mode）支持

| 功能 | 说明 |
|------|------|
| 开关粒度 | 以**号池级别**控制，开启后该号池下所有上游请求自动附带思考模式参数 |
| 支持范围 | 仅支持 OpenAI 兼容格式的模型厂商 |
| 实现方式 | 根据上游供应商类型，自动注入对应厂商的 thinking 扩展参数，详见"思考模式参数映射表" |
| 上游透传 | 代理层识别并透传给上游的真实模型，不改变原始请求结构，仅追加思考参数 |
| 兼容性 | 如果某家上游的模型不支持思考模式，自动降级为普通调用，不影响使用 |

**思考模式参数映射表**：

| 厂商 | 注入参数 | 参数类型 | 示例 |
|------|---------|---------|------|
| DeepSeek | `reasoning` | boolean | `"reasoning": true` |
| OpenAI / GPT | `reasoning_effort` | string (`"low"/"medium"/"high"`) | `"reasoning_effort": "high"` |
| Claude | `thinking` | object `{ "type": "enabled" }` | 通过扩展字段透传 |

**实现逻辑**：

1. 当号池 `thinking_enabled = true` 时，代理层根据当前选中的上游订阅所对应的供应商类型，从映射表中查找对应的 thinking 参数
2. 将该参数追加到发给上游的 JSON body 中
3. 若上游返回"不支持思考模式"的错误，代理层记录日志但不影响后续请求，仅在该次调用中降级为普通模式
4. 客户端无法感知思考模式差异，对外始终保持统一体验

```json
// 发送到上游的实际请求（以 DeepSeek 为例）
POST /v1/chat/completions
{
  "model": "deepseek-v4",
  "messages": [...],
  "reasoning": true   // ← 由代理层根据厂商自动注入
}
```

### 4.4 统一 API 网关（核心代理能力）

#### 4.4.1 功能描述

代理服务启动后，监听指定端口，对外提供 OpenAI 兼容的 HTTP API 接口。GUI 管理窗口由原生桌面应用提供，不依赖 Web 面板。

#### 4.4.2 网络参数

| 参数 | 默认值 | 说明 |
|------|--------|------|
| 监听地址 | `0.0.0.0` | 可配置为 `127.0.0.1` 或 `0.0.0.0` |
| 监听端口 | `47339` | 可配置，API Gateway 独立监听该端口 |
| Gateway API Key | `sk-gateway-key` | 默认值，可在配置中修改 |
| 管理方式 | **原生 GUI 窗口** | 程序启动时自动打开桌面窗口，无需浏览器 |

#### 4.4.3 终端工具对接方式

用户在任意客户端（如 ChatGPT Web、Claude Desktop、LibreChat、FastGPT 等）中配置：

```
Base URL: http://127.0.0.1:47339/v1
API Key:  sk-gateway-key
```

#### 4.4.4 支持的 OpenAI 兼容端点

| 端点 | 方法 | 说明 |
|------|------|------|
| `/v1/models` | GET | 返回所有已创建的号池名称作为可用模型列表 |
| `/v1/chat/completions` | POST | 统一完成接口，根据 model 字段匹配对应号池进行转发 |
| `/v1/embeddings` | POST | 可选支持，后续扩展 |
| `/v1/audio/transcriptions` | POST | 可选支持，后续扩展 |

#### 4.4.5 路由逻辑

1. 接收请求后，解析 `model` 字段
2. 在号池列表中查找匹配的号池名称
3. 找到后，从该号池中选择一条上游订阅进行转发
4. 将上游返回的响应原样转发给客户端（同时做模型名替换）
5. 如果未找到匹配的号池，返回 404 或明确的错误信息

#### 4.4.6 模型名替换

当用户选择模型 `grok-4.5` 时，实际发送给上游的模型名是该上游配置的实时模型（如 `deepseek-v4`、`glm-5.1`），上游返回的响应中如涉及 model 字段也统一替换为 `grok-4.5`，确保终端工具始终看到的是统一的模型名。

#### 4.4.7 流式响应（SSE）支持

| 场景 | 行为 |
|------|------|
| 客户端发送 `stream: false`（非流式） | 等待上游完整响应后一次性返回 |
| 客户端发送 `stream: true`（流式） | 代理层完整透传上游的 SSE 事件流，逐 chunk 回传给客户端 |

**SSE 透传策略**：
- 代理层不解析、不修改 SSE 事件内容，直接透传上游返回的 `data:` 行
- 每个客户端连接维护独立的 buffer，确保流式输出顺序正确
- 支持中断：如果客户端断开连接，代理层立即停止上游请求

### 4.5 轮询与故障转移机制

#### 4.5.1 轮询策略（当前版本）

采用 **顺序轮询（Round Robin）**：

- 每个号池内部维护一个当前指针
- 每次请求完成后，指针移向下一条上游订阅
- 到达最后一条后，回到第一条，形成闭环

```
号池 "grok-4.5":
  请求1 → 上游A (deepseek-v4)
  请求2 → 上游B (glm-5.1)
  请求3 → 上游C (gpt-5)
  请求4 → 上游A ...
```

#### 4.5.2 故障转移与错误溯源

- 当某个上游请求超时或返回 HTTP 5xx 错误时，自动跳过并尝试下一条上游
- 连续失败达到阈值（默认 3 次）后，将该上游标记为 **熔断** 状态
- 熔断一定时间（默认 60 秒）后自动恢复尝试
- 所有上游都失败时，返回友好的错误提示，告知用户检查上游配置

**错误溯源记录**：
- 每次触发故障转移时，系统会记录**具体是哪个上游（供应商 + 模型名）报错**以及错误原因（超时/HTTP 5xx/4xx/连接拒绝等）
- 管理面板「健康检查」和「请求日志」中可查看：
  - 哪个上游最近失败
  - 失败次数与最近一次失败时间
  - 错误类型与详细错误信息
- 支持手动标记某条上游为"有问题"，方便排查后手动禁用

```json
// 故障转移日志示例
{
  "request_id": "req_001",
  "pool_name": "grok-4.5",
  "original_upstream": {
    "provider": "DeepSeek",
    "model": "deepseek-v4",
    "error": "HTTP 500 - Internal Server Error"
  },
  "fallback_upstream": {
    "provider": "智谱GLM",
    "model": "glm-5.1",
    "status": "success"
  },
  "timestamp": "2026-07-25T10:05:00Z"
}
```

#### 4.5.3 并发控制与请求有序性保障

**问题描述**：若两个请求同时发给同一个上游，上游返回顺序可能和发出顺序不一致，导致 A 的回复拼到 B 的流里（流式场景尤其危险）。

**并发限制**：

| 配置项 | 说明 |
|--------|------|
| 最大并发数（Max Concurrency） | 每个号池可设置同时向该池内上游发出的请求上限，默认 5 |
| 并发队列 | 超过并发上限的请求进入等待队列，按客户端请求顺序排队 |

**示例**：用户将某号池的并发数设为 2。此时有 3 个请求到达：
- 请求 1、请求 2 立即分配给两个上游
- 请求 3 等待前两者之一释放

**防止响应乱序策略**：

1. **连接级有序保证**：每个号池内的每条上游，同一时刻只接受来自一个等待槽位的请求。请求完成后才释放该槽位，确保单个上游连接的请求不交叉。
2. **SSE 流式代理**：代理层为每个客户端请求维护独立的 SSE 流和 buffer，按收到上游 chunk 的顺序组装后回传，不因上游返回早晚会造成串流。
3. **并发数设 1 的兜底模式**：在配置中提供「严格串行」选项，开启后该号池所有请求依次执行，完全不并发，从根本上杜绝乱序。适用于对稳定性要求极高的场景。
4. **请求追踪 ID**：每个出站请求携带唯一 tracking_id，若检测到上游返回与当前客户端会话不匹配，立即丢弃并重新路由，防止数据泄露或串流。

**轮询与并发的交互**：

- 当并发数 > 1 时，round-robin 指针仅在**当前上游完成并被释放时**才移动，避免两个并发请求同时走同一条上游。
- 每个上游在并发窗口内独立计数，超出部分排队，保持负载均衡 fairness。

**熔断状态管理**：

| 状态 | 说明 |
|------|------|
| 正常（Healthy） | 可正常接收请求 |
| 熔断（Circuit Breaker） | 短时间内连续失败，暂时停止发送请求 |
| 半开（Half-Open） | 熔断时间过后，允许一次探测性请求 |
| 已禁用（Disabled） | 用户手动关闭该上游 |

### 4.6 原生桌面 GUI 管理窗口

#### 4.6.1 设计原则

- **原生桌面窗口**：程序启动后直接打开一个 GUI 窗口，所有操作都在这个窗口内完成
- **不需要打开浏览器**：不依赖 Web UI，不存在浏览器页面和命令行混杂的问题
- **窗口即控制台**：左侧或顶部导航切换模块，右侧显示对应内容，体验类似本地桌面软件

#### 4.6.2 窗口布局

```
┌─────────────────────────────────────────────────────┐
│ LLM-API-Proxy                          [最小化][关闭]│
├──────────┬──────────────────────────────────────────┤
│          │                                          │
│ 📊 仪表盘 │              当前选中模块内容               │
│          │                                          │
│ 🔌 上游   │                                          │
│          │                                          │
│ 🪟 号池   │                                          │
│          │                                          │
│ ⚙️ 网关   │                                          │
│          │                                          │
│ 📝 日志   │                                          │
│          │                                          │
│ 🩺 健康   │                                          │
│          │                                          │
├──────────┴──────────────────────────────────────────┤
│ 状态栏：运行中 | 监听 127.0.0.1:47339 | 上游5 | 号池3 │
└─────────────────────────────────────────────────────┘
```

#### 4.6.3 各模块功能

| 模块 | 功能 |
|------|------|
| 仪表盘 | 显示已配置上游数量、号池数量、今日请求统计、系统资源占用 |
| 上游管理 | 新增/编辑/删除/启用/禁用上游订阅；一键获取模型列表 |
| 号池管理 | 创建/编辑/删除号池；从上游订阅中选择并排序；设置思考模式、最大并发数 |
| 网关设置 | 配置监听端口、API Key、日志级别 |
| 请求日志 | 查看最近请求记录（时间、模型、上游、耗时、状态、错误溯源） |
| 健康检查 | 一键测试所有上游连通性 |

#### 4.6.4 配置持久化

- 使用 SQLite 数据库存储所有配置和日志
- 数据库文件位于程序同级目录下的 `data/` 文件夹
- API Key 在存入数据库前使用 AES-256-GCM 加密

---

## 五、数据模型

### 5.1 上游订阅表（upstreams）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | TEXT (PK) | UUID |
| provider_name | TEXT | 供应商名称 |
| base_url | TEXT | 上游 API 地址 |
| api_key_encrypted | TEXT | 加密后的 API Key |
| selected_model | TEXT | 实际模型名 |
| enabled | INTEGER | 是否启用（0/1） |
| remark | TEXT | 备注 |
| status | TEXT | 当前状态：healthy / circuit_breaker / disabled / error |
| failure_count | INTEGER | 连续失败次数 |
| last_failure_time | DATETIME | 最近一次失败时间 |
| created_at | DATETIME | 创建时间 |
| updated_at | DATETIME | 更新时间 |

### 5.2 号池表（pools）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | TEXT (PK) | UUID |
| name | TEXT (UNIQUE) | 号池名称 |
| display_name | TEXT | 对外展示的模型名 |
| round_robin_strategy | TEXT | 轮询策略：sequential |
| failover_enabled | INTEGER | 是否启用故障转移（0/1） |
| timeout_seconds | INTEGER | 超时阈值，默认 30s |
| max_concurrency | INTEGER | 最大并发数，默认 5 |
| thinking_enabled | INTEGER | 是否开启思考模式（0/1） |
| circuit_breaker_threshold | INTEGER | 熔断阈值，默认 3 |
| circuit_breaker_duration_seconds | INTEGER | 熔断时长，默认 60s |
| created_at | DATETIME | 创建时间 |
| updated_at | DATETIME | 更新时间 |

### 5.3 号池上游关联表（pool_upstreams）

| 字段 | 类型 | 说明 |
|------|------|------|
| pool_id | TEXT (FK) | 所属号池 |
| upstream_id | TEXT (FK) | 上游订阅 ID |
| order | INTEGER | 排序权重 |

### 5.4 请求日志表（request_logs）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | TEXT (PK) | UUID |
| request_id | TEXT | 请求追踪 ID |
| pool_name | TEXT | 命中的号池名 |
| upstream_id | TEXT | 最终成功的上游 ID |
| failed_upstreams | TEXT (JSON) | 失败的上游链及错误信息 |
| method | TEXT | HTTP 方法 |
| endpoint | TEXT | 端点路径 |
| status_code | INTEGER | 最终状态码 |
| response_time_ms | INTEGER | 响应耗时（毫秒） |
| is_streaming | BOOLEAN | 是否为流式请求 |
| created_at | DATETIME | 请求时间 |

---

## 六、接口设计

### 6.1 对外 API（OpenAI 兼容）

#### GET /v1/models

**请求：**
```http
GET /v1/models
Authorization: Bearer sk-gateway-key
```

**响应：**
```json
{
  "data": [
    {
      "id": "grok-4.5",
      "object": "model",
      "owned_by": "llm-api-proxy"
    },
    {
      "id": "gpt-5.5",
      "object": "model",
      "owned_by": "llm-api-proxy"
    },
    {
      "id": "claude opus 4.7",
      "object": "model",
      "owned_by": "llm-api-proxy"
    }
  ]
}
```

#### POST /v1/chat/completions

**请求：**
```http
POST /v1/chat/completions
Authorization: Bearer sk-gateway-key
Content-Type: application/json

{
  "model": "grok-4.5",
  "messages": [
    {"role": "user", "content": "你好"}
  ],
  "stream": true
}
```

**响应：** 透传上游模型的原始响应，其中 `model` 字段替换为 `grok-4.5`
- 非流式：完整 JSON 响应
- 流式：SSE 事件流，逐 chunk 返回

### 6.2 内部管理 API

#### 上游管理

| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/upstreams` | GET | 获取所有上游订阅 |
| `/api/upstreams` | POST | 新增上游订阅 |
| `/api/upstreams/{id}` | PUT | 更新上游订阅 |
| `/api/upstreams/{id}` | DELETE | 删除上游订阅 |
| `/api/upstreams/{id}/toggle` | PATCH | 启用/禁用 |
| `/api/upstreams/{id}/fetch-models` | POST | 一键获取模型列表 |

#### 号池管理

| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/pools` | GET | 获取所有号池 |
| `/api/pools` | POST | 创建号池 |
| `/api/pools/{id}` | PUT | 更新号池 |
| `/api/pools/{id}` | DELETE | 删除号池 |
| `/api/pools/{id}/test` | POST | 测试号池可用性 |

#### 网关设置

| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/settings` | GET | 获取网关配置 |
| `/api/settings` | PUT | 更新网关配置 |
| `/api/health` | GET | 系统健康检查 |
| `/api/logs` | GET | 获取请求日志 |
| `/api/logs/clear` | DELETE | 清空请求日志 |

---

## 七、非功能性需求

### 7.1 性能

| 指标 | 要求 |
|------|------|
| 单请求额外延迟 | ≤ 5ms（代理层开销） |
| 并发支持 | ≥ 100 QPS |
| 启动时间 | ≤ 3 秒 |

### 7.2 安全

- 默认仅监听 `127.0.0.1`，不对外网开放
- API Key 使用 AES-256-GCM 加密存储在 SQLite 数据库中
- 管理面板和网关入口使用相同的 API Key 认证
- 可选：启用 HTTPS（自签证书或 Let's Encrypt）

#### API Key 加密与便携存储方案

| 步骤 | 说明 |
|------|------|
| 密钥生成 | 首次启动程序时，自动生成一个随机 **Master Key**（32 字节），存储在 `data/master_key.bin` 文件中 |
| 加密方式 | 用户使用每个上游 API Key 时，使用 Master Key + AES-256-GCM 加密后存入 SQLite 的 `api_key_encrypted` 字段 |
| U盘跨电脑 | 因为 Master Key 跟随数据目录，U 盘插到任何电脑上，只要带着 `data/` 文件夹，API Key 都能正常解密 |
| 删除保护 | 删除整个程序目录即同时清除 Master Key 和加密数据，无法逆向恢复 API Key |

```
data/
├── proxy.db                 ← SQLite 数据库（配置+日志）
├── master_key.bin           ← 自动生成的一次性 Master Key
└── logs/                    ← 文本日志（可选）
```

**注意**：如果用户只移动了 `proxy.db` 而没有带走 `master_key.bin`，加密数据将无法解密。此时需要重新配置上游订阅。

### 7.3 便携部署

| 要求 | 说明 |
|------|------|
| 打包格式 | **单文件 .exe**，双击直接运行桌面 GUI 窗口 |
| U 盘 portable 模式 | 整个程序包放在 U 盘中，配置、数据、加密密钥全部跟随 exe 所在目录 |
| 跨电脑运行 | 拔到任意 Windows 电脑上，首次启动自动打开 GUI 窗口并初始化数据库，无需安装运行时 |
| 配置持久化 | SQLite 数据库文件位于程序同级目录下的 `data/` 文件夹，不写入系统目录 |
| 卸载方式 | 删除文件夹即完全清除，不注册系统服务、不写注册表、不打开浏览器 |
| 最小化到托盘 | 关闭窗口后程序最小化到系统托盘，后台继续提供 API 代理服务 |

```
U 盘/Laptop 目录结构示例：
LLM-API-Proxy/
├── LLM-API-Proxy.exe        ← 单文件可执行程序
├── data/                    ← 所有数据跟随便捷包
│   ├── proxy.db             ← SQLite 数据库（配置+日志）
│   └── logs/                ← 文本日志（可选）
└── README.md
```

### 7.4 可靠性

- **配置热加载**：通过 GUI 窗口修改配置（新增/编辑/删除上游或号池）后，代理引擎即时生效，无需重启服务。配置变更通过内部事件机制触发，不依赖文件监控
- 上游故障自动熔断，不影响其他上游
- 服务崩溃后自动从 SQLite 数据库恢复运行状态
- 数据库写入采用事务机制，确保配置一致性

### 7.5 兼容性

- 完全兼容 OpenAI SDK / 官方客户端协议
- **支持流式响应（SSE）**：代理层完整透传上游的 SSE 事件流，聊天体验无延迟、不缓冲
- 仅支持 OpenAI 兼容格式的模型厂商，不支持非标准格式
- 支持主流客户端直连：ChatGPT、Claude Desktop、LibreChat、FastGPT、Dify、Anything LLM 等

### 7.6 部署

- 支持 Windows 单机部署（主要目标）
- 提供单文件可执行程序
- 配置文件位于程序同级目录
- MVP 阶段仅支持 Windows，后续可扩展至 macOS/Linux

---

## 八、产品流程

### 8.1 首次使用流程

```
1. 双击 LLM-API-Proxy.exe 启动程序
   ↓
2. 自动打开原生 GUI 窗口
   ↓
3. 首次设置 Gateway API Key（GUI 内完成）
   ↓
4. 添加第一个上游订阅
   - 填写厂商名、URL、API Key
   - 一键获取模型列表
   - 选择模型并保存
   ↓
5. 创建号池 "grok-4.5"
   - 填入上游订阅
   - （可选）开启思考模式
   - （可选）设置最大并发数
   ↓
6. 关闭 GUI 窗口，程序最小化到托盘继续运行
   ↓
7. 在任何客户端中配置：
   - Base URL: http://127.0.0.1:47339/v1
   - API Key: sk-xxx
   - 模型: grok-4.5
   ↓
8. 开始使用
```

### 8.2 添加第二个上游到同一号池流程

```
1. 在上游管理中添加第二家供应商
   ↓
2. 一键获取模型列表
   ↓
3. 选择对应模型并保存
   ↓
4. 进入号池管理，编辑已有号池
   ↓
5. 将新的上游订阅添加到号池中
   ↓
6. 保存，轮询配置立即生效
```

### 8.3 创建多个号池

```
1. 重复上述流程，创建多个号池
   例：grok-4.5、gpt-5.5、claude-opus-4.7
   ↓
2. 每个号池绑定不同的上游组合
   ↓
3. 客户端中 /v1/models 会返回所有号池名称
   ↓
4. 按需选择不同号池名称进行对话
```

---

## 九、MVP 范围（第一期）

### 9.1 本期必做

- [x] 上游订阅的增删改查
- [x] 一键获取上游模型列表
- [x] 号池创建与管理
- [x] 统一 OpenAI 兼容入口
- [x] 顺序轮询
- [x] 基础故障转移（跳过失败上游）
- [x] 单页面管理面板（Dashboard）
- [x] SQLite 本地数据持久化
- [x] 请求日志查看
- [x] 思考模式（Thinking Mode）支持
- [x] 支持流式响应（SSE）
- [x] 单文件 .exe 便携部署（U 盘模式）
- [x] 并发数配置
- [x] 请求有序性保障（防止流式串流）
- [x] 错误溯源（记录具体哪个上游报错）

### 9.2 第二期规划

- [ ] 加权轮询 / 随机策略
- [ ] 上游价格与 Token 用量统计
- [ ] 多语言 / 多语言提示词路由
- [ ] 配额限制（每个号池每日调用上限）
- [ ] HTTPS 支持
- [ ] 配置文件导出/导入（备份迁移）
- [ ] Docker 部署支持

### 9.3 第三期规划

- [ ] AI 自动推荐号池分组
- [ ] 多用户权限管理
- [ ] 移动端管理 App
- [ ] 插件化上游适配器
- [ ] Webhook 告警

---

## 十、技术选型建议

| 模块 | 建议技术 | 说明 |
|------|----------|------|
| 后端框架 | Go / Node.js / Rust | 需高性能、低延迟；Go 推荐 |
| GUI 框架 | **Tauri**（Rust + HTML/CSS/JS）或 Electron | 原生桌面窗口，不开浏览器，窗口体验像本地软件 |
| 数据持久化 | **SQLite** | 单文件数据库，便携友好，MVP 首选 |
| 加密存储 | AES-256-GCM | API Key 加密后存入 SQLite |
| 部署形态 | **单文件 .exe**（Go/Rust + Tauri） | Windows 便携优先，数据全部跟随目录 |
| 流式代理 | SSE passthrough | 保持上游流式输出，客户端无感知 |
| 并发控制 | Go routine / channel 或 Node.js Promise pool | 实现信号量限流与请求追踪 |

---

## 十一、风险与应对

| 风险 | 影响 | 应对措施 |
|------|------|----------|
| 上游 API 格式不标准 | 部分厂商可能有非标准字段 | **本项目限定仅使用 OpenAI 兼容格式厂商**，排除非标风险 |
| 网络波动导致超时 | 用户体验下降 | 配置超时阈值 + 熔断机制 |
| API Key 泄露 | 安全风险 | 仅 localhost 监听 + AES 加密存储 + 可选 IP 白名单 |
| 大文件/长文本流式传输不稳定 | 聊天中断 | 支持 SSE 流式传输代理 |
| 免费额度被快速消耗 | 成本上升 | 后续增加用量统计和配额功能 |
| 并发请求导致流式响应乱序 | 内容串流、乱码 | 连接级有序保证 + 独立 SSE buffer + 请求追踪 ID |
| GUI 窗口与浏览器混用体验差 | 用户觉得混乱 | **全流程使用原生 GUI 窗口**，不依赖浏览器打开任何页面 |

---

## 附录 A：示例场景

### 场景一：日常对话

用户在客户端中选择 `grok-4.5`，每次请求自动在多个上游之间轮询。即使某家厂商暂时不可用，也能继续使用其他厂商的 `deepseek-v4` 或 `glm-5.1`。

### 场景二：批量任务

用户同时发起多个对话（不同号池），每个号池独立轮询，互不影响。

### 场景三：模型切换

用户创建两个号池：`fast`（DeepSeek-V4）和 `smart`（GPT-5.5），客户端中根据任务需求选择不同号池名称即可。

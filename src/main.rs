// Prevents additional console window on Windows in release, DO NOT REMOVE!!
#![cfg_attr(not(debug_assertions), windows_subsystem = "windows")]

fn main() {
    // M0: Placeholder — actual Tauri app will be wired when tauri crate is linked.
    println!("LLM-API-Proxy starting... (development placeholder)");
}
